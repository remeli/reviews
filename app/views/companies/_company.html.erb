<div class="company">
  <div class="title">
    <%= @company.name %>
  </div>
  <div class="extra">
    <p>Город: <%= link_to @company.city.name, @company.city %></p>  
    <p>Категория: 
      <%= link_to @company.category.name, city_with_category_path(@company.city, @company.category) %>
    </p>
    <p>
      Рейтинг: <%= render 'rating' %>
    </p>
  </div>
  <div class="description">
    <%= @company.description %>
  </div>
  <div id="add_photo_company">
    <%= link_to "Добавить фото", new_company_photo_path(@company) %>
  </div>
  <div class="dateadd_company">
    Дата добавления: <%= date(@company) %>
  </div>
  <%= owner?(@company) %>
</div>
<%= render 'map' %>